let gql;_9d4‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers]]);_9d4‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
    type Demand {
        id: ID!
        name: String!
        client: Client!
        deadline: String
    }

    extend type Query {
        demands: [Demand]!
    }
`;

       const resolvers = {
    Query: {
        demands: () => [],
    }
}