let gql;_49b‍.x([["typeDefs",()=>typeDefs],["ListSortmentEnum",()=>ListSortmentEnum],["resolvers",()=>resolvers]]);_49b‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
    interface List {
        itens: [Node!]!
        totalItens: Int!
    }

    enum ListSortmentEnum {
        ASC
        DESC
    }

    input ListSort {
        sorter: String!
        sortment: ListSortmentEnum! 
    }
`

       const ListSortmentEnum = Object.freeze({
    ASC: 'ASC',
    DESC: 'DESC',
});

       const resolvers = {
    List: {
        __resolveType: () => null,
    },
}

